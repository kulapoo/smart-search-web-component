export const filterOptionsStyles = `
  .ss-filter-options {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
`

export const filterOptionStyles = `
  .ss-filter-option {
    padding: 4px;
    border-radius: 4px;
    cursor: pointer;
  }
`
